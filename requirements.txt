# Semantic Search - Python Requirements
# ======================================
#
# Install all dependencies with:
#   pip install -r requirements.txt --break-system-packages
#
# Or individually:
#   pip install sentence-transformers --break-system-packages
#   pip install numpy --break-system-packages

# Sentence Transformers - For generating embeddings
# Downloads pre-trained model (~80MB first time)
sentence-transformers>=2.2.0

# NumPy - For vector operations
numpy>=1.21.0

# Note: sentence-transformers automatically installs:
# - transformers (Hugging Face)
# - torch (PyTorch)
# - tokenizers
# - tqdm (progress bars)
#
# Total download: ~500MB first time (models + dependencies)
# After first install: Everything is cached locally